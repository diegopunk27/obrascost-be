{
  "mcpServers": {
    "postgres": {
      "command": "node",
      "args": ["mcp-servers/postgresql/src/mcp-postgres-server.js"],
      "env": {
        "DB_HOST": "localhost",
        "DB_PORT": "5432",
        "DB_USERNAME": "postgres",
        "DB_PASSWORD": "your_password",
        "DB_DATABASE": "app_dev"
      }
    }
  }
}
